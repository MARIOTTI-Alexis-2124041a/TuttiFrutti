{% if is_granted('IS_AUTHENTICATED_FULLY') %}
    {{ component('SeeWhoIsLogged', {beforeName: this.beforeName , name: app.user.username }) }}
{% else %}
    {{ component('LogRegister', {logButtonName: this.logButtonName , registerButtonName : this.registerButtonName }) }}
{% endif %}